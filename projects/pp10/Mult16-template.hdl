//
// Hardware multiplier
//

/**
 * Multiplies two 8-bit values 0 <= a, b <= 255.
 * The result, interpreted as an unsigned number, is a * b.
 */

CHIP Mult16 {
    IN a[8], b[8];
    OUT out[16];

    PARTS:
    // Put you code here:
}
